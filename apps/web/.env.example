# Copy to .env.local and fill in (all optional for local dev)

# PostHog project key (free tier) - enables the full analytics funnel
NEXT_PUBLIC_POSTHOG_KEY=

# Resend API key (free tier, 100 emails/day) - production waitlist delivery
# MVP keeps emails local; wire this when deploying
RESEND_API_KEY=
